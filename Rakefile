$LOAD_PATH << File.expand_path("lib", File.dirname(__FILE__))
require 'resque/pool/tasks'

namespace :resque do
  task :setup do
    #RESQUE_POOL_CONFIG = {
    #  'foo'         => 1,
    #  'bar'         => 1,
    #  'baz'         => 1,
    #  'foo,bar,baz' => 4,
    #}
  end
end
